import { defineConfig } from 'astro/config';

// Published at https://albertkun.github.io/ice-impact-replication
// If this repo is ever renamed, update `base` to match the new repo name.
export default defineConfig({
  site: 'https://albertkun.github.io',
  base: '/ice-impact-replication',
});
